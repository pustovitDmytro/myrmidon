import { {{join helpers ' , '}} } from '{{info.name}}';

{{#examples}}
    {{#is this.type 'FunctionTester'}}
    {{this.function}}({{join this.input ' , '}}); // {{this.output}}
    {{/is}}
    {{#is this.type 'SnippetTester'}}
    {{this.input}}; // {{this.output}}
    {{/is}}
    
{{/examples}}
